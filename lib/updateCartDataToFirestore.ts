interface Props {
  id?: string;
  name?: string;
  email?: string;
  image?: string;
  emailVerified?: boolean;
}
const updateCartDataToFirestore = async (
  cartData: Product[] | [],
  email: string
) => {
  return [];
};

export default updateCartDataToFirestore;
